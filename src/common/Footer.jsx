import { Toolbar, Typography } from '@mui/material';

const Footer = () => {
	return (
			<Toolbar sx={{position:'sticky', bottom:0}}>
				<Typography variant='h5'>Footer</Typography>
			</Toolbar>
	);
};
export default Footer;
